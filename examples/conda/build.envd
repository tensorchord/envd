def build():
    install.conda_packages(name = [
        "pytorch",
        "cudatoolkit=11.3",
    ], channel= ["pytorch"])
    base(os="ubuntu20.04", language="python3.8",envFile="env.yaml")
    install.python_packages(name = [
        "flask"
    ])
